class: Progress
table: progress
fields:
  userId: UuidValue
  weeks: List<WeekSchedule>?, relation
  createdAt: DateTime, default=now
  updatedAt: DateTime, default=now

indexes:
  progress_user_unique_idx:
    fields: userId
    unique: true
