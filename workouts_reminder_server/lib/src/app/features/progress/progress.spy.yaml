class: Progress
table: progress
fields:
  authUser: module:serverpod_auth_core:AuthUser?, relation(onDelete=Cascade)
  weeks: List<WeekSchedule>?, relation(name=progress_weeks)
  createdAt: DateTime, default=now
  updatedAt: DateTime, default=now

indexes:
  progress_auth_user_unique_idx:
    fields: authUserId
    unique: true